fun find(peak)for(arr) {
    var rez = arr's 0;
    const arr_len = len(arr);
    if peak is not "minimum" and peak is not "maximum" {
        return -1;
    }
    loop 1..arr_len as it {
        if peak is "minimum" {
            if arr's (it) < rez {
                rez = arr's (it);
            }
        } else {
            if arr's (it) > rez {
                rez = arr's (it);
            }
        }
    }

    return rez;
}

print find("maximum")for([21,2,3,45, 67]);