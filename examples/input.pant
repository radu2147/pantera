fun compute_fibonacci_of_(len)_size {
    if len is 0 {
        return 0;
    }

    if len is 1 {
        return 1;
    }

    return compute_fibonacci_of_(len - 1)_size + compute_fibonacci_of_(len - 2)_size;
}

var a = input();

print compute_fibonacci_of_(atoi(a))_size;
